- name: Demo on Loops
  hosts: localhost
  tasks:
    - name: Print Debug
      ansible.builtin.debug:
        msg: Frunit name = {{item}}
      loop:
        - apple
        - banana
        - apple
        - orange
